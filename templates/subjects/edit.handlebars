<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Edit Course Management </title>
        <link rel="stylesheet" type="text/css" href="/css/subject/form_subject.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"></script>
    </head>
<body>
  <h1>Edit a Subject Of {{subject.name}}</h1>
  <form action="/subjects/{{subject._id}}?_method=PUT"  method="post">
    <label for="course-name">Course Name:</label>
    <input type="text" id="course-name" name="name" value="{{subject.name}}" placeholder="Enter Course Name...">
    
    <label for="course-code">Course Code:</label>
    <input type="text" id="course-code" name="code" value="{{subject.code}}" placeholder="Enter Course Code...">

    <label for="department">Department :</label>
     <select class="form-select" name="department" >
      
        {{#each departments}}
          <option  value="{{_id}}">{{name}}</option>
        {{/each}}
    </select>

         <label for="prerequisite">prerequisite :</label>
        <select class="form-select" name="prerequisite">
        {{#each subjects}}
          <option value="{{_id}}">{{name}}</option>
        {{/each}}
    </select> 
    {{!-- <input type="text" id="department" name="department" placeholder="Enter Your Department ..."> --}}

    {{!-- <label for="previous-course">Previous Required Course:</label>
    <input type="text" id="previous-course" name="previous-course" placeholder="Enter Previous Required Course..."> --}}
    
    <button type="submit" id="add-button">Edit</button>
  </form>
</body>
</html> 
